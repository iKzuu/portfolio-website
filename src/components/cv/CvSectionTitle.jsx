const CvSectionTitle = ({ children }) => {
    return (
        <h2 className="mt-4 border-b border-black pb-1 text-xs font-bold uppercase">
            {children}
        </h2>
    );
}

export default CvSectionTitle;